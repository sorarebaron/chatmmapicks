[theme]
primaryColor             = "#D20A0A"   # UFC red — buttons, links, active elements
backgroundColor          = "#0A0A0A"   # octagon black
secondaryBackgroundColor = "#1A1A1A"   # dark cage-steel for sidebar / cards
textColor                = "#F0F0F0"   # bright white
font                     = "sans serif"
